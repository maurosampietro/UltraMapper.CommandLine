using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;

namespace UltraMapper.CommandLine.UnitTest
{
    //[TestClass]
    //public class Commanding
    //{
    //    public class Commands
    //    {
    //        [Option( IsRequired = true )]
    //        public string A { get; set; }
    //    }

    //    [TestMethod]
    //    public void ImplicitCommanding()
    //    {
    //        //If we only have 1 defined command
    //        //or if we have marked a command with the DefaultCommand attribute
    //        //then we are not forced to use the --commandName notation. is this possible?

    //        var command = $"{nameof( Commands.A )}"; //notice we only pass the value, no command or namedparam notation

    //        var parsed = AutoParser.Instance.Parser.Parse( command );
    //        var target = AutoParser.Instance.Parse<Commands>( command );

    //        Assert.IsTrue( target.A == nameof( Commands.A ) );
    //    }
    //}

  
}
